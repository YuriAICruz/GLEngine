#include "gtest/gtest.h"

TEST(ECSTests, AddComponent)
{
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(ECSTests, GetComponent)
{
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(ECSTests, RemoveComponent)
{
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(ECSTests, Update)
{
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}
